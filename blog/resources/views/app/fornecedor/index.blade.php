<h3>Fornecedor</h3>

@php
    echo 'Teste';
@endphp

<!-- @if(count($fornecedores) > 0 && count($fornecedores) < 6)
    @dd($loop)
    <h3>Existem alguns fornecedores cadastrados</h3>
@elseif(count($fornecedores) >= 6) 
    <h3>Existem vários fornecedores cadastrados, total: {{count($fornecedores)}}</h3>
@else
    <h3>Não existe nenhum fornecedor cadastrado.</h3>
@endif -->